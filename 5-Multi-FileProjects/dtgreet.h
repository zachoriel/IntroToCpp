#pragma once

// Prints a greeting to the console based on the date
void dayGreeting(int day, int month, int year);

// Printa a greeting to the console based on a time
void timeGreeting(int hour, int minute);

bool isLeapYear(int years);